export enum InvoiceStatus {
    draft ='draft',
    sent = 'sent',
    current =  'current',
    late = 'late',
    paid = 'paid',
}

export enum ClientStatus {
    active = 'active',
    archived = 'archived'
}